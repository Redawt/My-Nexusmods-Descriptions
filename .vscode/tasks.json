{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Convert MD to BBCode",
            "type": "shell",
            "windows": {
                "command": "${workspaceFolder}\\.vscode\\tasks\\Convert-MDtoBB.ps1",
                "args": [
                    "${file}"
                ]
            },
            "options": {
				"cwd": "${workspaceFolder}"
			}
        }
    ]
}